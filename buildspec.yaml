version: 0.2

phases:
  pre_build:
    commands:
      - echo Nothing to do in the pre_build phase...
  build:
    commands:
      - echo Build started on `date`
      - mvn install

artifacts:
  files:
    - '**/hello-world-0.0.1-SNAPSHOT.war'
    - 'appspec.yaml'
  discard-paths: yes
